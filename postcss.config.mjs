export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
// This configuration file is for PostCSS, which is a tool for transforming CSS with JavaScript plugins.
// The file exports an object with a plugins property, which contains two plugins:
// 1. "@tailwindcss/postcss": This is the Tailwind CSS plugin for PostCSS, which allows you to use Tailwind CSS in your project.
// 2. "autoprefixer": This plugin automatically adds vendor prefixes to your CSS rules, ensuring better browser compatibility.